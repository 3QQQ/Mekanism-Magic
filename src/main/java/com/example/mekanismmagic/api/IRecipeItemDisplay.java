package com.example.mekanismmagic.api;

/** Exposes the item models represented by a machine's current recipe. */
public interface IRecipeItemDisplay {
    RecipeItemDisplayState mekanismMagicRecipeItemDisplay();
}
