package com.example.mekanismmagic.integration.arsnouveau;

import mekanism.common.block.attribute.Attribute;

/** Stores the eight-level Source-specific tier on a transmitter block. */
public record AttributeMagicSourcePipeTier(MagicSourcePipeTier tier)
        implements Attribute {
}
